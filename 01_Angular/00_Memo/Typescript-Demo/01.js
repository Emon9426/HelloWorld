// .ts后缀为一个typescript文件
//变量定义
//Typescript是强类型，可以不定义数据的类型，但是一旦定义了变量的类型则不能动态修改
var foo = 'bar';
console.log(foo);
//关于对象
var user = {
    name: 'Jack',
    age: 18
};
console.log(user.name);
console.log(user.age);
